const state = {
  user: {},
  userAvatar: null,
  invitations: []
};

  const mutations= {
    updateUser(state, user) {
      state.user = user;
    },
    updateUserAvatar(state, avatar) {
      state.userAvatar = avatar;
    },
  };

  export default{
  namespaced: true,
  state,
  mutations
}
