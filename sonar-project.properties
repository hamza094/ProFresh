# Basic project info
sonar.projectKey=hamza094_ProFresh
sonar.organization=hamza094
sonar.projectName=Profresh
sonar.projectVersion=1.0
sonar.sourceEncoding=UTF-8

# Limit analysis strictly to app and frontend source code
sonar.sources=.

# ✅ Only include relevant folders
sonar.inclusions=\
  app/**,\
  resources/js/**,\
  routes/**,\

# Optional: include specific files if needed
# sonar.inclusions=app/Services/**/*.php,app/Models/**/*.php

# (Optional) tests, if you want to track test quality too
sonar.tests=tests

# (Optional) coverage reports
 sonar.php.coverage.reportPaths=coverage.xml
