Rating factors
- on task =  
- on progect members 
- project note 
- activities count 
- stage id status 

public function score(){
	$score=0;

	$tasks=$project->tasks->count;
	$score=$score + $tasks*2;

	same for project members

	count if project note exists

	activities count

	return score;
}


 ->select('email','avatar_path','name','username','user_id')
              ->get()
             ->makeHidden('pivot');


table 

    if ($this->getMethod() == 'POST') {
        $rules += ['strtdt' => 'required'];
    }

    - record activity test
    - send notification.php 
    - dashboard.vue
    - activities.vue
    - stage.vue
    - page
    - query optimize
--------------------------    
    - api versioning
    - api responsible
    - profile page
    - socialite
    - payment
    - final refactoring and ui fix
    - deploy
    
